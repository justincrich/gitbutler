RED evidence from strengthening assertions in FIX-LOOP-001-FAKEABILITY-ASSERTIONS-2.

1. `cargo test -p but governed_loop_remediation_traversable -- --nocapture`
   failed after replacing substring denial checks with structured field checks because
   the test initially looked for `request a reviewed merge` in `error.message`.
   Actual parsed envelope placed that text in `error.remediation_hint`:
   `error.message == "action requires merge; authorization denied (held permissions: contents:write, pull_requests:write)"`.

2. `cargo test -p but governed_loop_remediation_traversable -- --nocapture`
   failed after adding the positive `pr new` forge-boundary oracle because the test
   initially accepted only `No authenticated GitHub users found`.
   Actual downstream boundary was:
   `No authenticated forge users found. Run 'but config forge auth' to authenticate with GitHub.`

3. `cargo test -p but governed_loop_reference_flow_full_loop -- --nocapture`
   failed after replacing substring denial checks with JSON parsing because reviewer
   commit denial emits the envelope as `Error: {"error":...}` instead of a separate
   `Caused by` JSON line. The parser was tightened to parse both CLI JSON-envelope
   layouts while still requiring valid JSON and exact fields.

These failures are from the strengthened tests themselves, before the helper fixes
landed. They demonstrate the tests now inspect the exact denial envelope and concrete
forge-boundary behavior instead of passing on broad substring absence.
