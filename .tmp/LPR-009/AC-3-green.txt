=== AC-3 GREEN: only verdict at head flips gate to PROCEED ===

Test: only_verdict_at_head_flips_gate
Command: cargo test -p but-api --test safe_seam_invariant only_verdict_at_head_flips_gate

Result: PASS — a single approved verdict at head makes the gate proceed.

Output:
---
test only_verdict_at_head_flips_gate ... ok
---

With NO drive rows (no assignment, no comment, no meta) and ONE approved verdict
at the current feat HEAD, enforce_merge_gate returned Ok(()). The gate reads
ONLY local_review_verdicts at head — the verdict-at-head is the sole input that
flips the land decision.
