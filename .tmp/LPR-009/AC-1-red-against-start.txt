=== AC-1 RED evidence: build-gate honesty grep discriminates ===

Test: merge_gate_path_has_zero_references_to_drive_tables
Discrimination proof: the grep catches a drive-table reference when one is present.

Simulated a gate path that illegally reads local_review_assignments:
--- /tmp/fake_gate_with_drive_ref.rs ---
fn check_review() {
    let assignments = db.local_review_assignments().list_by_target("feat");
}
---

Applying the same grep logic to this file:

Checked drive tables: ['local_review_assignments', 'local_review_comments', 'local_review_meta']
Violations found: ['local_review_assignments']
RESULT: FAIL — grep correctly detected forbidden reference(s): ['local_review_assignments']
The test discriminates — a real violation WOULD be caught.

Pre-impl RED: before this test file existed, there was NO static proof that the
merge-gate path (merge_gate.rs + review_requirement.rs) references none of the
three drive tables. The invariant was unproven at build time.

