AC-2 RED note:

`perm_revoke` with unset `BUT_AGENT_HANDLE` already failed closed on the stacked CLI-REM-001 base
because `enforce_administration_write_gate` resolves the caller through
`but_authz::resolve_principal_from_env`, which returns `Denial::no_handle`.

The CLI-REM-002 change for AC-2 is coverage hardening: the added integration test asserts the
structured `perm.denied` code, non-empty `remediation_hint`, and byte-for-byte unchanged
permissions file. The GREEN evidence is `.tmp/CLI-REM-002/AC-2-green.txt`, with seeded value
artifact `.tmp/CLI-REM-002/AC-2-seeded-artifact.txt`.
