AUTHZ-005 AC-1 red-against-start evidence.

The repaired contract was a composition test over AUTHZ-004 and AUTHZ-006. Those
gate behaviors were already present in the branch, so the required RED was
captured as the meaningful pre-repair expectation failure allowed by the prompt:
`cargo test -p but-api confinement_authority_from_config_not_claim` failed before
the test assertion was repaired.

Primary captured file: .tmp/AUTHZ-005/red-output.txt
Failure excerpt:
assertion `left == right` failed: RED: repaired contract requires the resolver to return reviewer
left: "reviewer"
right: "maint"

This proves the pre-repair expectation was red before the final AC-1 composition
test was kept green against the real merge gate and admin-write guard.
