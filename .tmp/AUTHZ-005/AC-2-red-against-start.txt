AUTHZ-005 AC-2 red-against-start evidence.

The repaired AC-2 replaced the impossible "denial message names reviewer" proof
with a positive resolver/persisted-principal assertion. Because the underlying
resolver behavior already existed, the prompt allowed a meaningful pre-repair
RED for the repaired assertion. That RED is captured in:
.tmp/AUTHZ-005/red-output.txt

Failure excerpt:
assertion `left == right` failed: RED: repaired contract requires the resolver to return reviewer
left: "reviewer"
right: "maint"

This red failure was produced before repairing the expectation to reviewer and
before the final CLI test asserted that --as maint is not accepted as an in-band
identity override.
