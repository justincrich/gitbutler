# Red-Hat Review Report — Sprint-07 LPR (Local Agent PR)

**Report Date**: 2026-06-22T17:35:10-07:00
**Target**: Sprint 07: Local Agent PR — Governed-Review Parity (LPR)
**Reviewed By**: 5 parallel reviewers — 3× rust-reviewer (backend), 1× general (Svelte UI), 1× general (Tauri producers)
**Master HEAD inspected**: `b8848c29fe` — confirmed via `git merge-base --is-ancestor` (the rust-reviewer-3 reference to `f7a1589c6c` was the active working branch, NOT master; all other reviewers correctly inspected `b8848c29fe`)
**SPRINT source**: `.spec/prds/governance/tasks/sprint-07-local-agent-pr/`
**Answer to the question**: **NO** — master does NOT have all functionality as defined in the sprint. 4 CRITICAL gaps and 3 HIGH gaps remain.

---

## Executive Summary

The **backend API layer** (LPR-001, LPR-002, LPR-003, LPR-005, LPR-007, LPR-008, LPR-011, LPR-013, LPR-015) is **real and tested** — every `#[but_api(napi)]` verb exists, gates on the correct `Authority`, writes to the correct drive table, and ships with passing tests. The **safe seam is intact at the source level** (merge_gate.rs / review_requirement.rs have zero references to the three new drive tables).

**However**, the sprint is not done. There are **4 CRITICAL gaps** — each independently sufficient to fail the SPRINT's own gate:

1. **LPR-004 CLI is STUB THEATRE** — `but review comment` is still wired to the dead `comment_review` stub that authenticates `CommentsWrite` then returns `task_contract_invalid` and writes nothing. `but review comments` and `but review resolve` CLI verbs do not exist at all. The real `post_comment`/`list_comments`/`resolve_thread` APIs are unreachable from the CLI.
2. **LPR-009 (the load-bearing safe-seam proof) is incomplete** — the no-read grep lives in `safe_seam_invariant.rs` instead of the required `invariant_build_gates.rs`; the bidirectional forged-vs-empty equivalence test is missing; the three-step capstone (T-LPR-040) is not chained. The SPRINT says: _"If this task is not green, the slice is NOT done, regardless of the other lanes."_
3. **LPR-016's LocalReviewView is an orphan component** — never mounted in `GovernanceSettings.svelte` or anywhere else in the live app. CT specs pass because Playwright mounts the component directly. SPRINT.md corroborates ("the MGMT desktop render of the local PR is deferred"), but LPR-016's status flag does not reflect this.
4. **LPR-010 is missing its CLI happy-path snapbox suite and the T-LPR-029 e2e full-local-loop capstone** — the thesis "the whole implement→review→merge loop runs ENTIRELY LOCAL with no forge" has no automated proof.

Plus 3 HIGH gaps: LPR-014's agent-badge read integration is missing (badge invisible in production), LPR-006 missing several named tests, LPR-010's `NAPI-AUDIT.md` artifact absent.

---

## AC VERDICT TABLE (Consolidated — 16 tasks)

| Task        | Title                                    | Verdict                                                                               | Critical issue                                                                                                                                                                                       |
| ----------- | ---------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LPR-001     | 3 tables + migrations + Handles          | ✅ **PASS** (6/6 ACs, 6/6 TCs)                                                        | —                                                                                                                                                                                                    |
| LPR-002     | `AssignmentState` enum                   | ✅ **PASS** (4/4 ACs, 4/4 TCs)                                                        | —                                                                                                                                                                                                    |
| LPR-003     | request/assign + changes_requested write | ✅ **PASS** (CLI wired for `request`/`assign`/`request-changes`; stub truly replaced) | —                                                                                                                                                                                                    |
| **LPR-004** | **comment / list / resolve verbs + CLI** | 🔴 **FAIL — STUB THEATRE**                                                            | `but review comment` hits dead `comment_review` stub; `but review comments` and `but review resolve` CLI verbs do not exist. API works; CLI doesn't.                                                 |
| LPR-005     | derived PR lifecycle + agent tag         | ✅ **PASS** (7/7 ACs)                                                                 | —                                                                                                                                                                                                    |
| LPR-006     | `keep_reviews_local: DefaultTrue`        | ⚠️ **PARTIAL**                                                                        | Field correct; AC-2/3/5 named tests not found. AC-4 named-seam test passes.                                                                                                                          |
| LPR-007     | `but-rules` review-requested hook        | ✅ **PASS** (5/5 ACs)                                                                 | —                                                                                                                                                                                                    |
| LPR-008     | reconciler read-API                      | ✅ **PASS** (5/5 ACs)                                                                 | —                                                                                                                                                                                                    |
| **LPR-009** | **safe-seam invariant (LOAD-BEARING)**   | 🔴 **FAIL — INCOMPLETE PROOF**                                                        | Grep in wrong file; bidirectional equivalence missing; 3-step capstone not chained.                                                                                                                  |
| **LPR-010** | **SDK regen + audit + CLI suite**        | 🔴 **FAIL — MISSING CLI + E2E**                                                       | CLI happy-path snapbox suite MISSING; T-LPR-029 e2e capstone MISSING; `NAPI-AUDIT.md` MISSING.                                                                                                       |
| LPR-011     | reconciler usage doc + skill contract    | ✅ **PASS** (3/3 doc ACs; human gate deferred by design)                              | —                                                                                                                                                                                                    |
| LPR-012     | `keep_reviews_local` UI toggle           | ✅ **PASS** (5/5 ACs, real RTK Query write, rendered live)                            | —                                                                                                                                                                                                    |
| LPR-013     | `principal_kind` Tauri producer          | ✅ **PASS** (5/5 ACs, admin-gated, SDK regen, all 15 tests pass)                      | —                                                                                                                                                                                                    |
| **LPR-014** | Principal `kind` editor UI               | ⚠️ **PARTIAL**                                                                        | Write path real and live; **READ integration missing** — `kind` not in `GovernancePrincipalListEntry`, agent badge never renders in production (CT-only).                                            |
| LPR-015     | local-review READ Tauri producer         | ✅ **PASS** (5/5 ACs, R14 no-bypass, branch-scoped, read-only)                        | —                                                                                                                                                                                                    |
| **LPR-016** | `LocalReviewView` panel                  | 🔴 **FAIL — ORPHAN COMPONENT**                                                        | Never mounted in `GovernanceSettings.svelte` or anywhere in `apps/desktop/src/`. Plus `ReviewStatus` payload lacks `source_branch`/`sha`/`author`/`title`/`created_at` per DESIGN-LPR-003 Section 1. |

**Totals**: 10 PASS · 2 PARTIAL · 4 FAIL (3 of which are CRITICAL) out of 16 tasks.

---

## HIGH Confidence Findings (3+ Reviewers Agree)

- [ ] **C1 — `but review comment` CLI verb is wired to the dead `comment_review` stub.** Reviewer 2 (rust-reviewer) cited `crates/but/src/command/legacy/forge/review.rs:103` calling `but_api::legacy::forge::comment_review` at `forge.rs:841`, which authorizes `CommentsWrite` then returns `task_contract_invalid`. Reviewer 3 independently flagged the missing `but review comments` / `but review resolve` CLI verbs. The real `post_comment` (forge.rs:880) exists, has passing tests, and is completely unreachable from the CLI. **Severity: CRITICAL.** The SPRINT explicitly says: _"`comment_review` WAS a contract stub … LPR-004's `post_comment` must REPLACE it (re-point the `but review comment` CLI verb at the real write). REPLACES — does not wrap."_ This was not done.
- [ ] **C2 — LPR-009 safe-seam proof is incomplete (LOAD-BEARING).** The no-read grep works at the source level (`rg merge_gate.rs review_requirement.rs` returns EMPTY), but the task spec mandates it live in `crates/but-authz/tests/invariant_build_gates.rs` using the shipped `assert_grep_has_no_matches` helper with a new `SAFE_SEAM_NO_READ` const + `REVIEW_REQUIREMENT` const + `SAFE_SEAM_GATE_PATHS` array. Instead it lives in `crates/but-api/tests/safe_seam_invariant.rs` using `fs::read_to_string`. The bidirectional forged-vs-empty equivalence test (`safe_seam_forged_drive_equals_empty_drive`) and the three-step capstone (`safe_seam_only_verdict_at_head_flips_the_land`) are MISSING. **Severity: CRITICAL.** SPRINT.md: _"If this task is not green, the slice is NOT done, regardless of the other lanes."_
- [ ] **C3 — LocalReviewView (LPR-016) is an orphan component.** Reviewer 4 (UI) verified via `git grep -ln "LocalReviewView" apps/desktop/src/` — the component is referenced ONLY by its own subtree (`LocalReviewAssignments.svelte`, `LocalReviewThreads.svelte`) and its CT spec. `GovernanceSettings.svelte:155-217` renders exactly 4 tabs (Principals / Groups / Branch Gates / Rules); there is no Local Review tab. The task spec's own Scope line says _"mounts in GovernanceSettings as the Local Review tab or panel"_ — that step was skipped. SPRINT.md corroborates (_"the MGMT desktop render of the local PR is deferred"_), but LPR-016's status flag claims Completed. **Severity: CRITICAL** for the surface; the component itself is real and the CT specs pass.

---

## MEDIUM Confidence Findings (2 Reviewers Agree)

- [ ] **M1 — LPR-010's CLI happy-path snapbox suite and T-LPR-029 e2e capstone are MISSING.** Reviewer 3 (rust) and the consolidated view confirm: no `lpr_review_cli_happy_path` test exists in `crates/but/tests/`; no `lpr_full_local_loop_request_to_merge_no_forge` capstone exists. The `governed_loop_reference_flow_full_loop()` test that does exist exercises `but pr new` + forge boundary, NOT the local-only `but review request → assign → comment → resolve → approve → but merge` flow with `keep_reviews_local=true`. The sprint's central thesis has no automated proof. **Severity: HIGH.**
- [ ] **M2 — LPR-014 agent badge is invisible in production (read integration missing).** Reviewer 4 (UI) and the Tauri reviewer converge: the write path (`principal_kind_update` IPC) is real, but `GovernancePrincipalListEntry` (`crates/but-api/src/legacy/governance.rs:216-227`) and the SDK type (`packages/but-sdk/src/generated/index.d.ts:1853-1862`) lack the `kind` field. The desktop's read path never calls `principal_kind_read` to merge it in. The `{#if principal.kind === "agent"}` branch in `PrincipalsList.svelte:184` is unreachable in production; CT specs bypass this by mocking the prop. **Severity: HIGH.**
- [ ] **M3 — LPR-016 payload schema gap.** `LocalReviewView.svelte:23-34` expects `source_branch/sha/author/title/created_at`; backend `ReviewStatus` (forge.rs:1018-1064) and the SDK type do not return these. DESIGN-LPR-003 Section 1 requires them. The component silently degrades via `{#if}` guards. Mitigated by C3 (the component never renders), but this must be closed before mounting. **Severity: HIGH.**
- [ ] **M4 — LPR-006 named tests missing.** `keep_reviews_local_persists_via_project_settings`, `agent_review_defaults_local_no_remote_pr`, `remote_pr_path_preserved_when_flag_false` are not found by name. Field definition and DefaultTrue are correct; only AC-4 has a dedicated test. Behavior may be correct but is under-proven. **Severity: MEDIUM.**

---

## LOW Confidence Findings (Single Reviewer)

- [ ] **L1 — `NAPI-AUDIT.md` artifact MISSING.** LPR-010 AC-4 requires a written R14 audit note at `.spec/prds/governance/tasks/sprint-07-local-agent-pr/NAPI-AUDIT.md`. The test `napi_audit_lpr_verbs_route_through_gated_but_api` exists and passes; the doc artifact does not.
- [ ] **L2 — Assertion message typo.** `assignment_state.rs:48` says `"changes_required"` in the message but checks `"changes_requested"`. Assertion itself passes; only the failure message is wrong.
- [ ] **L3 — Test file naming drift.** Task spec says `safe_seam.rs`; actual file is `safe_seam_invariant.rs`. Not a functional gap.
- [ ] **L4 — `close_review` is still a `task_contract_invalid` stub.** Not in LPR scope, but a related review verb that will bite later.
- [ ] **L5 — Naming drift on first lifecycle state.** Component uses "Draft" with "Open" alias; SPRINT.md prompt text uses "Open". Component handles both.
- [ ] **L6 — Both LPR-013 and LPR-015 shipped with `Reviewer: DEFERRED`.** This red-hat pass IS the first adversarial review.
- [ ] **L7 — No CI gate re-runs SDK regen + handler-registration assertions.** Future drift is possible.

---

## What's Real (Bright Spots — Do Not Re-Litigate)

1. **LPR-001**: Three tables (`local_review_assignments`, `local_review_comments`, `local_review_meta`) exist with exact methods named in the task. `SchemaVersion::Zero`, ids `20260621120000/1100/1200`, registered in `mod.rs:10-12` and `lib.rs:146-148`. 6/6 tests pass against real migrated `DbHandle`. No FKs (per spec).
2. **LPR-002**: `AssignmentState { Pending, Approved, ChangesRequested }` total round-trip; fail-closed on unknown. `Authority` enum unchanged (12 variants).
3. **LPR-003**: `request_review` / `assign_reviewer` / `request_changes_review` all `#[but_api(napi)]`, all wired to CLI verbs `request`/`assign`/`request-changes`. `request_changes_review` truly replaces the stub. Distinct-from-author (R22) enforced at `forge.rs:744`. Self-assignment rejected with structured `Denial`.
4. **LPR-005**: `is_agent_opener` at `forge.rs:1211-1221` correctly reads committed `permissions.toml` at the **target ref** via `load_permissions_wire(repo, target_ref)`, NOT handle-resolution, NOT a comment-body sentinel. `PrincipalWire.kind` added additively at `config.rs:513`, does not enter `GovConfig.principals`.
5. **LPR-006 field**: `Project.keep_reviews_local: DefaultTrue` at `project.rs:117-122`, mirrors `ok_with_force_push: DefaultTrue`. Correct.
6. **LPR-007**: `Trigger::Commit` (`lib.rs:111`) and `Action::RequestReview(RequestReviewAction)` (`lib.rs:166`) genuinely extend the shipped enums. Handler creates pending assignment, visible in `review_status`. 5/5 tests pass.
7. **LPR-008**: `review_status` serves full drive state (assignments + unresolved threads + verdict-at-head) in one payload. Two-read convergence test passes.
8. **LPR-012**: `KeepReviewsLocalToggle.svelte` rendered live via `ProjectSettingsModalContent` → `GeneralSettings`. Real RTK Query `updateProject` mutation. Two-way binding, error revert, R21 caveat copy verbatim.
9. **LPR-013**: `principal_kind_read` / `principal_kind_update` registered in Tauri `invoke_handler!`, admin-gated, SDK regen with 3 DTOs. All 15 tests pass.
10. **LPR-015**: `review_status` / `list_comments` registered, branch-scoped (F-006), read-only (byte-unchanged proof), R14 no-bypass (zero direct `local_review_*()` reads in `crates/gitbutler-tauri/src/`).
11. **LPR-011**: `RECONCILER-USAGE.md` exists with `keep_reviews_local` skill contract and 24 R18-R23 residual mentions.
12. **Safe seam at source**: `rg "local_review_assignments|local_review_comments|local_review_meta" crates/but-api/src/legacy/merge_gate.rs crates/but-api/src/legacy/review_requirement.rs` returns ZERO matches. The gate path is honest at the implementation level.

---

## Recommendations by Category

1. **Gaps (BLOCKING — must close before slice is "done")**:
   - **Close C1** (LPR-004 CLI): re-point `crates/but/src/command/legacy/forge/review.rs:103` from `comment_review` to `post_comment`; add `Comments` and `Resolve` subcommands to `crates/but/src/args/forge.rs`; add `--file`/`--line`/`--thread` options to `Comment`; dispatch in `crates/but/src/lib.rs`; remove `#[but_api(napi)]` from the dead `comment_review` (or delete it). Add CLI happy-path snapbox tests.
   - **Close C2** (LPR-009 safe-seam): add `SAFE_SEAM_NO_READ`, `REVIEW_REQUIREMENT`, `SAFE_SEAM_GATE_PATHS` to `crates/but-authz/tests/invariant_build_gates.rs` using `assert_grep_has_no_matches`; add `safe_seam_drive_rows_have_no_effect_on_satisfied_merge`, `safe_seam_forged_drive_equals_empty_drive` (bidirectional), and the chained `safe_seam_only_verdict_at_head_flips_the_land` 3-step capstone.
   - **Close C3** (LPR-016 orphan): wire `<LocalReviewView>` into `GovernanceSettings.svelte` as a new "Local Review" tab — OR honestly mark LPR-016 as partially complete and create a follow-up task for the mount. **The latter is what SPRINT.md already implies** ("MGMT desktop render deferred"); the task status flag should match.
   - **Close M1** (LPR-010 capstones): add `lpr_review_cli_happy_path` snapbox suite and `lpr_full_local_loop_request_to_merge_no_forge` e2e capstone.
2. **Risks (should close before public release)**:
   - **Close M2** (LPR-014 read integration): extend `GovernancePrincipalListEntry` with `kind: Option<String>` or wire `principal_kind_read` into `GovernanceSettings.svelte`'s read path.
   - **Close M3** (LPR-016 schema): extend backend `ReviewStatus` with `source_branch/sha/author/title/created_at`, OR relax DESIGN-LPR-003 Section 1.
3. **Assumptions to revisit**:
   - The deferred-reviewer sign-off on LPR-013/015 means this report is the first adversarial pass; consider a second focused review after remediation.
4. **Contradictions**: None between reviewers. The only disagreement was Reviewer 3's HEAD reference (`f7a1589c6c` = working branch, not master); all other reviewers correctly inspected `b8848c29fe` and `git merge-base --is-ancestor` confirms `b8848c29fe` IS master HEAD.

---

## Agent Reports (Summary)

- **rust-reviewer-1** (LPR-001/002/009): 22 ACs evaluated. 16 PASS, 2 PARTIAL, 4 FAIL. 7 CRITICAL findings (S1/S2/G1/G2/G3/G4/G9) all on LPR-009. LPR-001 and LPR-002 fully clean.
- **rust-reviewer-2** (LPR-003/004): 19 ACs evaluated. API surface 100% PASS and 12/12 tests green. CLI layer for LPR-004 is STUB THEATRE — 1 CRITICAL (`but review comment` hits dead stub) + 2 HIGH (missing verbs) + 2 MEDIUM.
- **rust-reviewer-3** (LPR-005/006/007/008/010/011): 32 ACs evaluated. LPR-005/007/008/011 fully PASS. LPR-006 PARTIAL (named tests missing). LPR-010 has 2 CRITICAL gaps (CLI suite, e2e capstone).
- **general-reviewer-4** (LPR-012/014/016 UI): 16 ACs evaluated. LPR-012 fully PASS. LPR-014 write path PASS, READ path BROKEN. LPR-016 component real but ORPHAN — never mounted. Schema gap on `ReviewStatus` payload.
- **general-reviewer-5** (LPR-013/015 Tauri): 10 ACs + 4 TCs evaluated. All PASS. No stubs. No bypasses. All commands registered. SDK regen verified.

---

## Metadata

- **Agents**: rust-reviewer ×3, general ×2 (sveltekit/tauri reviewer specialists not in opencode roster)
- **Confidence Framework**: HIGH (3+ agents), MEDIUM (2 agents), LOW (1 agent)
- **Report Generated**: 2026-06-22T17:35:10-07:00
- **Master HEAD verified**: `b8848c29fe18a92adf9cd968d89cff2ff40ff350` (verified via `git merge-base --is-ancestor b8848c29fe master`)
- **Next Steps**: **Remediate C1, C2, C3, M1 before claiming the slice is done.** C2 is load-bearing per the SPRINT's own gate ("If LPR-009 is not green, the slice is NOT done"). C1 makes the LPR-004 CLI surface a facade. C3 means an entire UI surface is invisible. M1 means the central thesis is unproven.
