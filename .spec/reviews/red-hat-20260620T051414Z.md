# Red-Hat Review Report

## AC Verdict Table

**Completion Gate:** `needs-revision` — CLI-002 has blocking spec-coverage gaps. This is a task-contract review; implementation evidence is not expected yet because `SPRINT.md` states implementation is out of scope for this artifact.

| Task | AC | Verdict | Evidence | Notes |
|---|---:|---|---|---|
| CLI-001 | AC-1 | PASS | `CLI-001-perm-cli-verbs.md:96` | Real working-tree write, target-ref unchanged, ref-pin caveat. |
| CLI-001 | AC-2 | PASS | `CLI-001-perm-cli-verbs.md:104` | Role sugar and unrelated entries are checked by value. |
| CLI-001 | AC-3 | PASS | `CLI-001-perm-cli-verbs.md:112` | First grant seeds a principal and proves post-commit authorization. |
| CLI-001 | AC-4 | PASS | `CLI-001-perm-cli-verbs.md:120` | Revoke and idempotent no-op are covered, with minor wording ambiguity in embedded JSON. |
| CLI-001 | AC-5 | PASS | `CLI-001-perm-cli-verbs.md:128` | Non-admin grant and revoke denial plus unchanged file. |
| CLI-001 | AC-6 | PARTIAL | `CLI-001-perm-cli-verbs.md:136` | Strong for `perm_grant`; `perm_revoke` bad-token/unset-handle and unset-handle `perm_list` are not required. |
| CLI-001 | AC-7 | PASS | `CLI-001-perm-cli-verbs.md:144` | Resolved-known non-admin no-leak scope check is strong. |
| CLI-001 | AC-8 | PASS | `CLI-001-perm-cli-verbs.md:152` | PENDING diff must come from committed-vs-working comparison. |
| CLI-002 | AC-1 | PASS | `CLI-002-group-cli-verbs.md:88` | Add-member write is inert and caveat is asserted. |
| CLI-002 | AC-2 | PARTIAL | `CLI-002-group-cli-verbs.md:96` | Create/grant happy path is covered; duplicate-create error is prose only. |
| CLI-002 | AC-3 | FAIL | `CLI-002-group-cli-verbs.md:104` | AC claims all four mutating verbs; scenario/TCs only exercise `group_add_member`. |
| CLI-002 | AC-4 | PARTIAL | `CLI-002-group-cli-verbs.md:112` | Good for `group_grant`; missing related fail-closed cases for `group_create` and add/remove paths. |
| CLI-002 | AC-5 | PASS | `CLI-002-group-cli-verbs.md:120` | Admin-read positive and no-admin-read denial are covered. |

Validator note: extracted embedded JSON contracts passed `/Users/justinrich/Projects/brain/tools/validate-scenario/validate_scenario.py`: CLI-001 `ok=true`, 8 scenarios, 0 violations; CLI-002 `ok=true`, 5 scenarios, 0 violations. The findings below are gaps the validator cannot see because the scenario set itself is incomplete.

## Executive Summary

The sprint contracts are generally strong and fakeability-resistant for `but perm`, but `but group` overclaims coverage. The blocking issue is CLI-002 AC-3: the task says all mutating group verbs must be denied for a non-admin, while the concrete test path only exercises `group_add_member`, allowing ungated `group_create`, `group_grant`, or `group_remove_member` to pass the sprint.

## HIGH Confidence Findings (3+ Agents Agree)

- [ ] CLI-002 AC-3 overclaims non-admin coverage for mutating group verbs. | Severity: Critical
  Agents: `security-auditor`, `rust-reviewer`, `code-reviewer`
  Evidence: `CLI-002-group-cli-verbs.md:104` names `group_create/grant/add-member/remove-member`, but the concrete path only calls `group_add_member` at `CLI-002-group-cli-verbs.md:106` and TCs at `CLI-002-group-cli-verbs.md:141`.

- [ ] `group_remove_member` has no positive behavior AC. | Severity: High
  Agents: `rust-reviewer`, `code-reviewer`, supported by `security-auditor`'s verb-matrix finding
  Evidence: scope includes `group_remove_member` at `CLI-002-group-cli-verbs.md:17`, but verification gates at `CLI-002-group-cli-verbs.md:227` do not require a successful remove-member mutation.

- [ ] Delegated admin via `group_grant administration:write` is mandatory in prose but optional in tests. | Severity: High
  Agents: `security-auditor`, `rust-reviewer`, `code-reviewer`
  Evidence: `CLI-002-group-cli-verbs.md:66` says it MUST be allowed; `CLI-002-group-cli-verbs.md:251` makes the positive test optional.

- [ ] Duplicate group create is mentioned but not an executable AC/TC. | Severity: Medium
  Agents: `security-auditor`, `rust-reviewer`, `code-reviewer`
  Evidence: duplicate-create error appears in negative-control prose at `CLI-002-group-cli-verbs.md:102`, but the scenario creates only a new `code-reviewers` group.

- [ ] `group_delete` scope/stub policy is contradictory. | Severity: Medium
  Agents: `security-auditor`, `rust-reviewer`, `code-reviewer`
  Evidence: `SPRINT.md:125` allows a flagged fast-follow stub; `CLI-002-group-cli-verbs.md:69` says never ship delete in this task.

## MEDIUM Confidence Findings (2 Agents Agree)

- [ ] CLI-001 AC-6 omits fail-closed coverage for `perm_revoke` and unset-handle `perm_list`. | Severity: High
  Agents: `security-auditor`, `rust-reviewer`
  Evidence: AC-6 at `CLI-001-perm-cli-verbs.md:136` only exercises `perm_grant`, despite token parsing/caller resolution constraints at `CLI-001-perm-cli-verbs.md:69`.

- [ ] Structured denial ACs do not assert `remediation_hint`. | Severity: Medium
  Agents: `security-auditor`, `rust-reviewer`
  Evidence: sprint requires `{code, message, remediation_hint}` at `SPRINT.md:55`; task ACs mostly assert `code` and sometimes `message`.

- [ ] CLI target-ref resolution is under-tested. | Severity: High
  Agents: `rust-reviewer`; accepted as medium confidence after consolidation because other reviewers did not independently raise it.
  Evidence: CLI shims must resolve workspace target ref, not HEAD, at `CLI-001-perm-cli-verbs.md:25` and `CLI-002-group-cli-verbs.md:21`; CLI tests do not require HEAD != target.

## LOW Confidence Findings (Single Agent)

- [ ] Unknown-principal `perm_list` existence oracle is not covered. | Severity: Medium
  Agent: `security-auditor`
  Evidence: AC-7 at `CLI-001-perm-cli-verbs.md:144` covers known target `maint`, not an unknown principal with uniform denial.

- [ ] Guard composition is specified but not mechanically proven. | Severity: High
  Agent: `rust-reviewer`
  Evidence: specs require `enforce_administration_write_gate` at `CLI-001-perm-cli-verbs.md:64` and `CLI-002-group-cli-verbs.md:60`; the honesty grep proves Authority-axis use, not that exact guard composition.

- [ ] CLI-001 Done When boxes are pre-checked despite implementation being out of scope. | Severity: Medium
  Agent: `rust-reviewer`
  Evidence: `CLI-001-perm-cli-verbs.md:82`-`90` are `[x]`, while `SPRINT.md:149` says these are TDD task contracts and Rust is written later.

- [ ] CLI-001 AC-4 embedded JSON capture order is ambiguous. | Severity: Low
  Agent: `code-reviewer`
  Evidence: the idempotent revoke snapshot wording around `CLI-001-perm-cli-verbs.md:541` should clearly capture after the first revoke and before the not-held revoke.

## Agent Contradictions & Debates

| Topic | Agent A | Agent B | Assessment |
|---|---|---|---|
| CLI-001 AC-6 | `security-auditor`/`rust-reviewer`: PARTIAL | `code-reviewer`: PASS | Keep as MEDIUM confidence gap: the grant path is strong, but revoke/list fail-closed variants are not mandatory. |
| CLI-002 AC-4 | `security-auditor`: PARTIAL | `rust-reviewer`/`code-reviewer`: PASS | Keep as PARTIAL because `group_grant` is covered well, but sibling verb fail-closed matrix is incomplete. |
| CLI-001 AC-4 | `security-auditor`: PARTIAL | `rust-reviewer`/`code-reviewer`: PASS | Treat as LOW risk wording ambiguity, not a blocker. |

## Recommendations by Category

1. **Gaps:** Add required CLI-002 AC/TC rows for non-admin `group_create`, `group_grant`, and `group_remove_member`, plus a positive `group_remove_member` AC.
2. **Risks:** Make delegated admin via `group_grant administration:write` a required test, not optional, and assert inert-until-committed.
3. **Assumptions:** Add tests for duplicate `group_create`, `perm_revoke` bad-token/unset-handle, unset-handle `perm_list`, and `remediation_hint`.
4. **Contradictions:** Remove the Sprint 05 `group_delete` stub allowance or explicitly declare no delete surface in Sprint 05.

## Agent Reports (Summary)

- **rust-reviewer:** 8 findings: CLI-002 verb matrix, missing remove-member positive, target-ref under-test, guard-composition under-test, denial hint gap, delegated-admin gap, delete contradiction, prechecked CLI-001.
- **security-auditor:** 8 findings: CLI-002 non-admin matrix, CLI-001 fail-closed variants, delegated admin, unknown-principal recon, remediation hint, duplicate/undefined group management, group_create fail-closed, delete contradiction.
- **code-reviewer:** 5 findings: CLI-002 non-admin matrix, missing remove-member positive, delegated-admin optionality, duplicate-create prose-only, delete contradiction, plus low-risk AC-4 wording.

## Metadata

- **Report Date:** 2026-06-20T05:14:14Z
- **Target:** Sprint 05 CLI `but perm` / `but group` (`.spec/prds/governance/tasks/sprint-05-cli-perm-group`)
- **Reviewed By:** `rust-reviewer`, `security-auditor`, `code-reviewer`, orchestrator consolidation
- **Confidence Framework:** HIGH (3+ agents), MEDIUM (2 agents or single high-signal reviewer finding), LOW (single agent)
- **Report Generated:** `.spec/reviews/red-hat-20260620T051414Z.md`
- **Duration:** not separately measured by harness
- **Next Steps:** Remediate findings before approving the sprint contract.
