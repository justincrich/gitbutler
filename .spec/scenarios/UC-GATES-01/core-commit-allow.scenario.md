---
feature: UC-GATES-01
type: happy_path
tier: integration-test
---

# UC-GATES-01 — core-commit-allow

As a contents:write principal (dev), run but commit on a feature branch → exit 0, the branch ref advances. The gate authorizes contents:write and the target is not protected.
