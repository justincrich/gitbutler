---
feature: UC-GATES-01
type: security
tier: integration-test
---

# UC-GATES-01 — edge-readonly-denied

As a read-only principal (ro, contents:read only), run but commit → denied exit 1, perm.denied names contents:write.
