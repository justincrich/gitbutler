---
feature: UC-GATES-01
type: security
tier: integration-test
---

# UC-GATES-01 — edge-protected-branch

As dev (contents:write), run a direct commit to protected main → denied exit 1, branch.protected (branch protection is enforced regardless of write authority).
