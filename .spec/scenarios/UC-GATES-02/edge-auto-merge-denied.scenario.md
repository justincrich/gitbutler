---
feature: UC-GATES-02
type: security
tier: integration-test
---

# UC-GATES-02 — edge-auto-merge-denied

As an implementer lacking merge authority, enable auto-merge (but review --auto-merge) → denied exit 1, perm.denied (the auto-merge path is gated identically to the manual merge path).
