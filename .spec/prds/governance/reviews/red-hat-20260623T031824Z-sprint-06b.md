# Red-Hat Review Report — Sprint 06b "Governance UI — Branch Gates + Rules + Safety"

**Report Date:** 2026-06-23T03:18:24Z
**Target:** `.spec/prds/governance/tasks/sprint-06b-governance-ui-branch-gates-rules-safety/`
**Reviewer:** direct evidence-gathering pass (file:line verdicts; no subagent dispatch — every AC is structurally verifiable from HEAD)
**Verdict:** ❌ **NOT DONE — `needs-revision`** (4 of 7 FEATURE UI tasks unimplemented; 0 of 11 AC boxes checked across the sprint; 14 remediation/E2E follow-up tasks Backlog)

---

## Executive Summary

The backend producer layer (MGMT-BE-003/004) and the design contract layer (DESIGN-MGMT-004/006/007/008) are landed at HEAD. The ErrorBoundary wrap (MGMT-UI-004) is in place at the mount point. **Everything else is missing**: the namesake `BranchGatesList.svelte` does not exist; `RulesList.svelte` has no `principalId` prop; `TabTrigger.svelte` still carries the inverted-`tabindex` bug MGMT-UI-011 was created to fix; `TabList.svelte`/`TabContent.svelte` carry no aria attributes; no build-gate test script exists; and 14 follow-up task files (8 REMEDIATE-_, 1 REM-_, 4 E2E-\*, plus E2E-MGMT-BE-002A) sit at `Status: Backlog`. The Human Testing Gate cannot be run because two of its six steps exercise UI that does not exist. Sprint is **not done**.

---

## AC VERDICT TABLE — Per Task

### MGMT-BE-004 — `branch_gates_read`/`branch_gates_update` gate-config writer

| #   | AC Item                                                  | Verdict    | Evidence                                                                                                                                                 | Notes                                                                                                                                                                              |
| --- | -------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Admin write hits working tree, target-ref read unchanged | ✅ PASS    | `crates/but-api/src/legacy/governance.rs:555,563,641,664`                                                                                                | Producer exists; inert-until-committed pair plumbed.                                                                                                                               |
| 2   | Toggling `protected=false` lands, preserves `[[gate]]`   | ⚠️ PARTIAL | `crates/but-api/src/legacy/governance.rs:671`                                                                                                            | Writer exists; lossless round-trip NOT verified at this review layer (needs the AC-3 GREP proof + a wire-struct snapshot). Reviewer must run `cargo test -p but-api branch_gates`. |
| 3   | Lossless round-trip of full `[[gate]]` set               | ⚠️ PARTIAL | —                                                                                                                                                        | Needs test evidence; REMEDIATE-RUST-1 (GatesWire widening) is `Backlog` — verify it's folded in or still pending.                                                                  |
| 4   | SDK regenerated; Tauri command wired                     | ✅ PASS    | `crates/gitbutler-tauri/src/governance.rs:245,585,593`; `packages/but-sdk/src/generated/index.d.ts:106,109`; `crates/gitbutler-tauri/src/lib.rs:188-218` | SDK + Tauri command + fleet-owner resolver all in place.                                                                                                                           |
| —   | AC checkboxes in task file                               | ❌ FAIL    | `MGMT-BE-004-…md` (every `[ ]` still unchecked)                                                                                                          | No AC has been checked off despite implementation.                                                                                                                                 |

### MGMT-BE-003 — `principalId`-scoped rules query

| #   | AC Item                                        | Verdict | Evidence                                                                                                                                | Notes                                     |
| --- | ---------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| 1   | `scoped(None)` ≡ `list_workspace_rules`        | ✅ PASS | `crates/but-api/src/legacy/rules.rs:73`; test `crates/but-api/tests/rules_scoped.rs:7-13`                                               | Delegate-by-construction proven.          |
| 2   | `scoped(Some(id))` narrows                     | ✅ PASS | `crates/but-api/tests/rules_scoped.rs:39-66`                                                                                            | Agent-A vs agent-B narrowing test exists. |
| 3   | Unknown principal → empty                      | ✅ PASS | `crates/but-api/tests/rules_scoped.rs:67-82`                                                                                            | Clean empty state.                        |
| 4   | Tauri + SDK governed path                      | ✅ PASS | `crates/gitbutler-tauri/src/lib.rs:55`                                                                                                  | Tauri command wired.                      |
| 5   | Cross-principal scope honest (self/admin-read) | ✅ PASS | `crates/but-api/src/legacy/rules.rs:89-112`; `crates/gitbutler-tauri/tests/list_workspace_rules_scoped.rs:23` (REMEDIATE-RUST-3 landed) | Recon-leak closer is in.                  |
| —   | AC checkboxes                                  | ❌ FAIL | All `[ ]` in `MGMT-BE-003-…md`                                                                                                          | Box-checking step skipped.                |

### MGMT-UI-004 — Wrap `GovernanceSettings` in existing `ErrorBoundary`

| #   | AC Item                                 | Verdict    | Evidence                                                                     | Notes                                                                                                                                                  |
| --- | --------------------------------------- | ---------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | ErrorBoundary catches governance throw  | ⚠️ PARTIAL | `apps/desktop/src/components/views/ProjectSettingsModalContent.svelte:68-70` | Wrap is in place (`title="Governance settings failed to load" compact={false}`); no CT evidence the throw → fallback path is exercised by a real test. |
| 2   | No new `GovernanceErrorBoundary.svelte` | ✅ PASS    | `glob apps/desktop/src/**/GovernanceErrorBoundary*` → 0 results              | Build-gate target met.                                                                                                                                 |
| 3   | Wrapper transparent on normal render    | ⚠️ PARTIAL | —                                                                            | No CT asserting "0 `.boundary-error` + 4 tab triggers visible".                                                                                        |

### MGMT-UI-009 — `BranchGatesList.svelte` ❌ **NOT STARTED**

| #   | AC Item | Verdict | Evidence                                                                                                                                                                                                                                                                                                 | Notes                                                                                                                                       |
| --- | ------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 1–7 | All ACs | ❌ FAIL | `glob apps/desktop/src/**/BranchGatesList*` → **0 results**. The Branch Gates tab is a stub: `apps/desktop/src/components/governance/GovernanceSettings.svelte:224-236` renders only `<button>Add gate</button>` inside a `<section>`. No SDK call, no ExpandableSection, no Modal, no denial surfacing. | **The namesake component of the sprint does not exist.** Human Testing Gate step 1 (toggle Protected, see pending indicator) is impossible. |

### MGMT-UI-010 — `RulesList` `principalId` prop ❌ **NOT STARTED**

| #   | AC Item | Verdict | Evidence                                                                                                                                                                                                                                                              | Notes                                                              |
| --- | ------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 1–5 | All ACs | ❌ FAIL | `apps/desktop/src/components/rules/RulesList.svelte:20-22` — `type Props = { projectId: string; }`. **No `principalId` prop.** `grep principalId apps/desktop/src/lib/rules` → 0 results. The Rules tab body is a stub button at `GovernanceSettings.svelte:238-250`. | Human Testing Gate step 2 (select principal A vs B) is impossible. |

### MGMT-UI-011 — Accessibility + IPC retry + denial no-flip ❌ **NOT STARTED**

| #   | AC Item                                  | Verdict    | Evidence                                                                                                                                                                                                                                                                      | Notes                                                                                                                                                                   |
| --- | ---------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Aria-labels + keyboard nav               | ❌ FAIL    | `apps/desktop/src/components/shared/TabList.svelte:11` — bare `<ul>`, **no `role="tablist"`, no `aria-label`, no ArrowLeft/Right/Home/End handler**. `apps/desktop/src/components/shared/TabContent.svelte:18` — `<div>` with **no `role="tabpanel"`, no `aria-labelledby`**. | Direct conflict with the MUST items in the task contract.                                                                                                               |
| 1b  | Inverted tabindex FIX                    | ❌ FAIL    | `apps/desktop/src/components/shared/TabTrigger.svelte:25` — still reads `tabindex={isActive ? -1 : 0}` (the BUGGY form). Task MUST clause (b) requires `tabindex={isActive ? 0 : -1}`.                                                                                        | The exact bug this task exists to fix is still live.                                                                                                                    |
| 2   | IPC failure → danger InfoMessage + Retry | ⚠️ PARTIAL | `apps/desktop/src/components/governance/GovernanceSettings.svelte:133-148` has a Retry InfoMessage, but it is wired only to `pendingStore.error` (initial load failure) — NOT to write-path IPC failures (no write paths exist yet to fail).                                  | Retry pattern exists in primitive form; full UC-MGMT-07 coverage absent.                                                                                                |
| 3   | Persistent failure keeps UI read-only    | ❌ FAIL    | —                                                                                                                                                                                                                                                                             | No proof; no test.                                                                                                                                                      |
| 4   | Self-escalation denial no-flip           | ❌ FAIL    | —                                                                                                                                                                                                                                                                             | No Toggle in the governance surface is wired to a write that can return `perm.denied`. Consumer-side no-bypass proof (the load-bearing UC-MGMT-06 invariant) is absent. |
| 5   | Read-only disables all write controls    | ⚠️ PARTIAL | `GovernanceSettings.svelte:151-160` shows the read-only InfoMessage; `disabled={isReadOnly}` appears on the two stub "Add gate"/"Add rule" buttons only — there are no real write controls to disable.                                                                        | Vacuously true today; becomes meaningful only when 009/010 land.                                                                                                        |

### MGMT-UI-012 — Build-gate test suite ❌ **NOT STARTED**

| #   | AC Item                                                        | Verdict    | Evidence                                                                                                          | Notes |
| --- | -------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------- | ----- |
| 1   | No direct `.gitbutler/*.toml` write                            | ❌ FAIL    | `grep` in `package.json` / `apps/desktop/package.json` → **no such gate script exists**                           | —     |
| 2   | No `+page.server.ts` under `apps/desktop/src`                  | ⚠️ PARTIAL | `glob apps/desktop/src/**/+page.server.ts` → 0 (target met by absence), but **no gate enforces it going forward** | —     |
| 3   | SDK type-check gate                                            | ❌ FAIL    | no script                                                                                                         | —     |
| 4   | Fleet-owner shim present + `resolve_principal_from_env` absent | ❌ FAIL    | no gate script                                                                                                    | —     |
| 5   | No `GovernanceErrorBoundary.svelte`                            | ⚠️ PARTIAL | absence holds at HEAD; no gate enforces it                                                                        | —     |

### DESIGN-MGMT-004 / 006 / 007 / 008 — Design contracts ✅ **DONE**

| Task                                         | Verdict | Evidence                                                                                                                                              |
| -------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| DESIGN-MGMT-004 (denial banner + no-flip)    | ✅ PASS | `apps/desktop/src/components/governance/DESIGN-ANNOTATIONS.md:415,431,469,481` — InfoMessage danger, banner text verbatim, three-state machine table. |
| DESIGN-MGMT-006 (empty states)               | ✅ PASS | DESIGN-ANNOTATIONS.md covers all four tabs + Branch Gates empty state verbatim.                                                                       |
| DESIGN-MGMT-007 (four-tab a11y contract)     | ✅ PASS | DESIGN-ANNOTATIONS.md references WAI-ARIA Tabs pattern, tab IDs in canonical order.                                                                   |
| DESIGN-MGMT-008 (error-boundary + IPC retry) | ✅ PASS | DESIGN-ANNOTATIONS.md:311,492,493 — two-category distinction (boundary catch vs IPC), persistent-failure safe read-only state.                        |

---

## GAPS & RISKS

1. **CRITICAL — The sprint's namesake UI does not exist.** `BranchGatesList.svelte` is unimplemented; the Branch Gates tab is a button-only stub (`GovernanceSettings.svelte:224-236`). The sprint's Human Testing Gate step 1 is unverifiable. This is the single largest blocker.
2. **CRITICAL — Consumer-side no-bypass proof (UC-MGMT-06 / T-MGMT-030) is absent.** No Toggle in the governance surface is wired through `branch_gates_update` such that a `perm.denied` response can be shown without flipping the control. The "load-bearing invariant" is asserted in prose and design only — never demonstrated in UI behavior. The entire reason this sprint exists (visible proof that the renderer never optimistically applies an authority change the server would refuse) is not delivered.
3. **HIGH — The exact bug MGMT-UI-011 exists to fix is still live in `TabTrigger.svelte:25`.** Active tab has `tabindex=-1` (unfocusable via Tab key); inactive tabs have `tabindex=0`. This is the inverted form; keyboard users cannot reach the active tab via Tab key. Direct conflict with task MUST clause (b).
4. **HIGH — Rules tab cannot be scoped by principal.** `RulesList.svelte` Props type is unchanged; `rulesService` has no `principalRules` method. Backend producer (`list_workspace_rules_scoped`) exists but has no consumer. Human Testing Gate step 2 is unverifiable.
5. **HIGH — No build-gate test suite (MGMT-UI-012).** The honesty greps that enforce "no direct `.gitbutler/*.toml` write from renderer", "no `+page.server.ts`", "fleet-owner shim present", "SDK type-check" are not in the repo. Future regressions will sail through CI.
6. **MEDIUM — Zero AC checkboxes are marked complete across all 11 task files.** Even tasks with passing evidence (MGMT-BE-003/004, MGMT-UI-004) have all boxes `[ ]`. The `/kb-run-sprint` completion ledger is out of sync with HEAD. The Sprint status in `SPRINT.md` line 5 still reads `Planned`.
7. **MEDIUM — 14 follow-up task files sit at `Backlog`.** 8 REMEDIATE-_, 1 REM-_, 4 E2E-\*, plus E2E-MGMT-BE-002A. None are in the SPRINT.md task table; none are executed. The red-hat loop that produced them ran, but the remediations haven't.
8. **MEDIUM — REMEDIATE-RUST-1 (GatesWire widening for lossless round-trip) status unclear.** The writer exists but whether the `[[gate]]` array is now round-tripped losslessly needs `cargo test -p but-api branch_gates` evidence at review time. AC-3 of MGMT-BE-004 is unverified.
9. **LOW — CT harness inherited from Sprint 06a is assumed but not re-checked.** Every UI `[component-test]` AC depends on the T-MGMT-000 desktop CT harness carried by MGMT-UI-001. No evidence in this sprint that the harness is live and these tests run.

---

## ASSUMPTIONS (Unvalidated)

- **"Sprint is sequenced 8 / final POC sprint"** — SPRINT.md `Blocks: None`, but 14 follow-up tasks in the directory contradict the "final" framing. Either the follow-ups are out-of-sprint backlog (in which case the sprint boundary is a lie) or they are in-sprint (in which case the SPRINT.md task table is incomplete). Resolve which.
- **"Backend producers being landed means UI tasks can run"** — true at the SDK contract level, but the absence of BranchGatesList/RulesList changes means the SDK delta is **unconsumed** at HEAD. Type-check passes against generated types, but no runtime caller exercises the new SDK surface.
- **"Desktop CT harness from 06a exists"** — none of the 06b UI CT tests can run without it; not verified by this review.

---

## CONTRADICTIONS

| Topic                             | Position A                                   | Position B                                              | Assessment                                                                                                                                                             |
| --------------------------------- | -------------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sprint status                     | SPRINT.md:5 `status: Planned`                | HEAD has backend + design + 1 UI task landed            | Status field is stale; either reset on each run or never updated.                                                                                                      |
| "Final sprint of the POC roadmap" | SPRINT.md:104 `Blocks: None (final sprint…)` | 14 Backlog follow-up tasks in the same directory        | If the follow-ups are POC scope, sprint isn't final; if not, they belong in 06c/07.                                                                                    |
| Human Testing Gate readability    | SPRINT.md:73-76 describes 6 verifiable steps | Steps 1, 2, 4, 6 require UI that does not exist at HEAD | Gate description is aspirational; only steps 3 (read-only banner) and 5 (keyboard nav) are even partially runnable, and step 5 fails because the tabindex is inverted. |

---

## CONFIDENCE SUMMARY

- **HIGH confidence:** 7 findings (the 4 NOT-STARTED UI tasks, the inverted tabindex, the missing build-gate suite, the unchecked AC ledger)
- **MEDIUM confidence:** 2 findings (REMEDIATE backlog, REMEDIATE-RUST-1 lossless round-trip unverified)
- **LOW confidence:** 1 finding (CT harness assumption)

---

## Recommendations by Category

1. **Gaps** — Land `MGMT-UI-009` (BranchGatesList), `MGMT-UI-010` (RulesList principalId), `MGMT-UI-011` (a11y + IPC retry + denial no-flip), `MGMT-UI-012` (build-gate tests) in that dependency order. These four tasks are the entire remaining implementation surface.
2. **Risks** — Wire at least one Toggle through `branch_gates_update` to deliver the UC-MGMT-06 consumer-side no-bypass proof. Without it, the sprint's load-bearing claim is undocumented in behavior.
3. **Assumptions** — Verify the 06a CT harness runs (`pnpm test:ct -- Governance`) before claiming any UI AC PASS. Run `cargo test -p but-api branch_gates` to convert MGMT-BE-004 AC-3 from PARTIAL to PASS/FAIL.
4. **Contradictions** — Update SPRINT.md `status:` field. Decide whether the 14 follow-up tasks are in-sprint (add to task table) or out-of-sprint (move to a 06c/07 directory). Reconcile "final sprint" with reality.

---

## Completion Gate

**ANY FAIL or PARTIAL → `needs-revision`.**

This sprint has 4 FAIL tasks and 5 PARTIAL tasks. **Sprint is NOT done.**

Estimated remaining work: 4 UI tasks (≈225 min per SPRINT.md estimates) + AC box-checking on the 5 landed tasks + sprint bookkeeping (status, task table, follow-up triage).

---

## Metadata

- **Review mode:** Direct evidence-gathering (file:line verdicts against HEAD `design/lpr-ui-design-contracts` @ f7a1589c6c). No subagent dispatch — every AC is structurally verifiable, so parallel review would re-discover the same evidence at higher cost.
- **Confidence Framework:** HIGH (multiple file:line proofs), MEDIUM (single proof + inferred), LOW (assumption)
- **Report Generated:** 2026-06-23T03:18:24Z
- **Next Steps:** [Remediate the 4 NOT-STARTED UI tasks | Update SPRINT.md status + AC ledgers | Triage 14 Backlog follow-ups into or out of this sprint]
