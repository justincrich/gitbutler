---
stability: FEATURE_SPEC
last_validated: 2026-06-20
prd_version: 1.0.0
scope_posture: full
---
# Scope

**Scope Posture**: Full feature (a complete, polished v1 slice — deliberately scoped tight).

The boundary sentence for every item: **Check Runner makes a change unmergeable until every required check has a `success` bound to the current head OID, produced by a butler-controlled local runner that is not the agent, recorded in a plain store, and read mechanism-agnostically from the head commit — with a STEER denial on any miss.** It is the verification PRODUCER half; the governance merge gate is the CONSUMER (a new required-checks clause); STEER is the redirect. Everything opinionated about *which* checks run lives in committed, ref-pinned config; the engine ships the mechanism only — a real runner, a plain result store, and a deterministic gate clause.

## The bar is "honest path is cheapest," not "cheating is impossible" (constraint, not a feature)
The threat model is **personal-tenant, own-fleet**: trusted human, trusted machine, an agent fleet the human runs. The goal is to make the **honest path** (let the runner run the check) **cheaper than cheating**, because a reward-hacking agent optimizes for least resistance. Two cheap things carry it: **reproducibility** (a forged green is **detectable** by re-running — though only *post-merge*, see Known Limitations; the honest path is cheap to take **for a fast, deterministic check**) and the **structural locks** (the gate reads a stored fact, not agent prose; runner ≠ agent; no caller-supplied-conclusion API; SHA-binding). v1 has **no runtime isolation and no cryptographic non-forgery layer** — and is consistent in this with the governance review clause, which already gates merges on a verdict store it accepts as forgeable (its R6). A check is another deterministic review that is **safer in one dimension** (reproducible → post-merge lie-detection) but **weaker in another** (a check row carries no principal identity, so forgery isn't deterred by attribution the way impersonating a named reviewer is) — so its store needs no *more* protection than the review store, but the asymmetry rests on reproducibility + the structural locks, **not** on accountability.

## The composition assumption (constraint, not a feature)
Check Runner does **not** re-invent the gate. The required-checks clause **extends the existing governance merge gate** (`.spec/prds/governance/`), evaluated as part of the same `enforce_merge_gate` code alongside the `merge`-authority and review clauses. It reuses governance's denial contract (`{code, message, remediation_hint}` + the STEER fields `class`/`held_permissions`/`authorized_actions`/`do_not`), its ref-pin discipline (config read at the target ref), and its fail-closed posture. This PRD specifies the producer (the runner), the contract (the plain result store), and the new clause (the gate). Two governance-side dependencies are named, not assumed: the STEER steering fields the denial reuses are delivered by governance **STEER-001** (sprint-07, **unmerged** — the GATE group sequences after it), and a **mechanism-agnostic local-merge entry point** must exist for the clause to gate a local `but merge` (the shipped `enforce_merge_gate` is forge-`review_id`-keyed — see [00-overview.md](./00-overview.md) and `08-technical-requirements/`).

## In Scope

### Check definition (config-as-code) — DEFN
- **Named checks** in committed, ref-pinned `.gitbutler/checks/*.toml`: each = a stable `name` (the analog of a GitHub status `context`) + a `trigger` + a `run-spec` + a `required` flag + a success mapping.
- **Triggers** restricted to two: `on-commit` (eligible when a commit is created) and `on-merge-attempt` (the trusted CLI/daemon runs the required checks immediately before a governed merge). No cron / dispatch / external event.
- **Run-spec**: a **command** string **or** a repo-local `./path` script — **local resolution only**, butler-run. No `uses` registry, no marketplace, no remote-ref / Docker / JS / composite resolution.
- **Success mapping**: process exit `0` → conclusion `success`; non-zero → `failure` — a deterministic function of the real run, never an agent's claim.
- **Required-set policy** (`[[required_check]]` in governance `gates.toml`, read at the target ref, mirroring the `[[gate]]` review requirement): names which defined checks are mandatory for a branch's merge gate. A defined check not in the required-set runs and records but does not block.
- **Ref-pinned at the target ref** (exactly like `gates.toml`), so a change cannot weaken, drop, or flip `required: false` on a required check **in its own head** to slip the gate; and the required-set is **self-protecting** — a change to it must itself clear the currently-required checks (the **bootstrap-invariant**).

### Check runner (the producer) — RUN
- A butler-controlled **local runner** that obtains a **clean checkout at the exact current head OID**, runs the defined `run-spec`, captures stdout/stderr + the exit code, and maps it to a typed conclusion.
- **Runner ≠ the gated agent**: it runs in the **trusted daemon/CLI the human runs**, NOT the agent's process — the load-bearing structural lock (whoever runs the check controls the result). It exposes **no path by which the agent supplies a conclusion**.
- **Mechanism-agnostic**: the checkout/binding works identically across GitButler **virtual branches**, **worktrees**, and **plain git** — it binds to the head *commit OID*, never assuming the live worktree == the head OID, and never disturbing or contending on the agent's shared worktree.
- **Real execution, no stubbed success — ever**: the runner actually invokes the command/script; a stubbed pass, a fake conclusion, or a skip-that-records-green is the cardinal sin, not a v1 simplification. A check it cannot run concludes **closed** (`failure`/`timed_out`), never `success`.
- **Result bound to the head OID**, recorded by deterministic engine code in a **plain `but-db` `check_results` table** (recording a result is never an agent decision). The result is keyed `(name, head_oid)`.
- **SHA-reset by construction**: a result is valid for its head OID only; any graph/ref mutation that moves the head OID leaves the new head with no satisfying result until the check is re-run — a green never carries across OIDs.
- **Timeout / concurrency / observability**: a check exceeding `timeout_secs` is terminated and concluded `timed_out`; concurrent runs key on `(name, head_oid)` and never clobber; a run emits an observable event with **dual-audience** output (human CLI + machine `--json`). (Checks that need butler-held credentials — secret injection — are **deferred**; v1 runs checks that need no secret. See Out of Scope.)

### Required-checks merge gate (the consumer) — GATE
- A **required-checks clause**: a change may merge into the target **iff** for **every** required check name (from the target-ref `[[required_check]]` policy) there is a **`success` bound to the current head OID** in `check_results`.
- **On-merge-attempt is a pre-merge run + read-only gate**: the trusted CLI/daemon runs the required `on-merge-attempt` checks immediately before the governed merge; the gate then consumes the current-head results. The gate itself **never runs a check** — a stale-at-head required check makes it BLOCK with `check_stale_at_head` + STEER ("run the check"), not auto-re-run.
- **Composes** with the governance clauses (same `enforce_merge_gate`, alongside `merge`-authority + review) — process *and* quality in one decision.
- **Fail-closed**: a required check that is **missing**, **failed** (`failure`/`timed_out`/`cancelled`), **stale** (a result exists for the check but bound to a non-current head OID), or whose target-ref config is **unreadable/malformed** ⇒ **merge blocked** (`config.invalid` for a bad config), never vacuously satisfied. Enforced **independent of the branch's `protected` flag** — a branch carrying required checks is gated even if it is not in the governance *protected* set (no fail-open via the protected-branch early-return).
- **STEER denial**: on a miss, the gate returns the governance-consistent `{code: "gate.check_required", message, remediation_hint}` + the steering fields — `class` (`ActorCorrectable` for a runnable miss; `OperatorRequired` for `config.invalid`), `authorized_actions` (e.g. `{command: "but check run <name> --head <oid>", effect: "produce the missing check result"}`), and `do_not` where applicable — distinguishing miss-reasons (`check_missing` / `check_failed` / `check_stale_at_head` / `config.invalid`). Dual-audience (`--json` for the orchestrator).
- **Bootstrap-invariant**: a change that edits the required-set or a required check's definition must itself clear the currently-required checks at the target ref before it can land.
- **Mechanism-agnostic gate entry**: the clause must be reachable from a **local `but merge`** (virtual-branch / worktree / plain-git, no forge PR) that resolves `(source_ref, target_ref, head_oid)` **without a forge `review_id`**. The shipped `enforce_merge_gate` is currently forge-review-keyed, so generalizing its entry (head OID via a mechanism-agnostic `gix` ref-peel) is a v1 requirement, not an assumption.

### Human-facing UI (desktop, v1.1) — UI
v1.0 is CLI-first; **v1.1 un-defers a focused desktop result-viewing surface** (grounded in a `frontend-designer` review of the real `apps/desktop/` UI — see [`08-technical-requirements/10-frontend-ui.md`](./08-technical-requirements/10-frontend-ui.md)). GitButler today renders forge checks only as a single aggregate `CIChecksBadge` that links out to GitHub; the per-check `CiCheck[]` data is fetched and **cached** (the `listCiChecks` query caches `CiCheck[]`), but every current consumer reads it through `ChecksMonitor`'s `parseChecks` call-site transform that collapses it to an aggregate — so nothing renders the per-check detail today. In scope for **v1.1**:
- A **per-head `CheckResultsPanel`** (the visual form of `but check results`, UC-RUN-05) **+ a "Checks" segment** on the existing `[projectId]/branches` explorer (real segments today: `All / PRs / Local` from `BRANCH_FILTER_OPTIONS = ["all","pullRequest","local"]`) — a **state of an existing route, not a new route**. Net-new: `CheckConclusionBadge` + `CheckResultRow` + `CheckResultsPanel`, all composing existing `packages/ui` primitives. **Modest, not free**: the panel requires a query-path change (a parallel `listCiChecks` without the `parseChecks` transform, or a `ChecksMonitor` refactor) and type-system surgery to add the "Checks" segment (extend `BRANCH_FILTER_OPTIONS`, the `BranchFilterOption` union, `isBranchFilterOption`, the `filterOptions` block in `BranchExplorer.svelte`, and a new `{type:"checks";headOid}` arm in the `BranchesSelection` union in `BranchesView.svelte`).
- Deferred (named): the **merge-gate summary** (UC-GATE — blocked on governance's own deferred merge dialog, and sequentially on Surface 1 above), the **Checks settings tab** (UC-DEFN — needs the DEFN CLI first; also requires governance settings tab wiring to be confirmed), the **cross-branch checks matrix** (needs a `but check results --all-branches` capability), and any **lite (React) checks UI** (no checks UI exists in `apps/lite/`; `packages/ui` is Svelte-only → a **port**, not a share).

## Out of Scope

### Deferred (named as out-of-threat-model, NOT owed debt)
- **Cryptographic non-forgery** — signing as security, an agent-unwritable hardened ledger, HMAC/Ed25519, an OS-sandboxed runner. Not needed under the own-fleet reproducibility bar; a nullable `signature` column is retained only as a forward-compat seam for off-host producers, explicitly not a v1 security claim. *If* the model later changes (untrusted contributors, remote/multi-machine producers), that seam is where provenance-signing earns its place.
- **Untrusted / fork-contribution execution** — running a hostile contributor's check safely (the `pull_request` vs `pull_request_target` split, ephemeral hostile-code sandboxing). v1 runs the repo's own committed, trusted check definitions only.
- **A detached/remote runner protocol** — lease / long-poll / labels / runner-groups / autoscaling. v1 runs synchronously in the trusted local daemon/CLI.
- **Action types beyond local** — JavaScript / Docker / composite action types, a `uses` registry / marketplace, remote-ref (`owner/repo@sha`) resolution. v1 is a command or a repo `./path` script.
- **Workflow orchestration** — matrix / strategy, a job DAG (`needs`), reusable workflows. v1 is a flat list of named checks.
- **A full expression engine** — the `${{ }}` contexts/functions. v1 maps an exit code to a conclusion.
- **Merge-queue / "require up to date"** — serialized landing + "head must be at/after target tip." v1 evaluates required checks at the current head; the gate-eval-vs-merge race is named in Known Limitations.
- **Consuming third-party forge CI as a first-class required producer** — `ci_checks` already caches forge check-runs; wiring it in as a second producer behind the same clause is the natural next layer (the `check_results` contract is designed to admit it), but v1 ships the local runner as the sole producer.
- **Secret injection for checks that need credentials** — a `secrets`/`env` declaration on a check + scoped injection + output masking. v1 runs checks that need no butler-held secret (tests / lint / typecheck); a check needing a private-registry token etc. is deferred (and would re-introduce the masking/runtime-exfiltration caveats GitHub also carries).
- **Rich result UX** — annotations (file+line), live log-streaming, a results dashboard. v1 captures the gate-critical conclusion + a log link.
- **The deferred UI surfaces** — the merge-gate summary (blocked on governance's deferred merge dialog), the Checks settings tab (needs the DEFN CLI first), the cross-branch checks matrix, and any **lite (React)** checks UI (Svelte-only library → a port). The v1.1 result-viewing surface (per-head panel + branches "Checks" state) is now **in scope** (above); the agent-facing surface stays CLI (`but check …`).

### Genuinely out (non-goals)
- **Certifying semantic code quality** — the gate proves the committed check ran and exited 0, never that the code is *correct* or the check is a *good* test.
- **Driving the agent's reasoning** — Check Runner governs *whether a validated change may land*, never the agent's prompts or turn loop (the harness owns the agent).
- **Closing governance's fence leak** — raw `git` / `--no-verify` / an ungoverned direct N-API call still bypass the governed `but` merge (the same accepted-leak class as governance R1/R14). The clause binds the **governed** merge only.

## Known Limitations

| Limitation | Impact | Mitigation in v1 | Follow-up |
|---|---|---|---|
| **The gate certifies "the committed check ran and exited 0," not "the code is good"** | A weak/vacuous check yields a weak guarantee | The strength is the committed check set's; the required-set is human-reviewed + self-protecting (bootstrap-invariant); the engine guarantees the check really ran on this OID | Better checks (a user concern), not engine machinery |
| **Mechanism-agnostic clean checkout at the head OID is the hard part** | GitButler is virtual-branches-over-one-worktree; the live tree is dirty and is not a clean checkout of one branch's head, so "run in the repo dir" would silently break the head-OID binding | An **isolated checkout at the head OID** (throwaway/warm `git worktree add --detach`, or a `gix`/object-DB-only run for purely-git checks) that never touches the agent's shared worktree; a named per-`on-merge-attempt` latency budget | tmpfs / warm-pool optimization; see `08-technical-requirements/07-mechanism-agnostic-checkout.md` (the #1 risk) |
| **No non-forgery guarantee — a determined agent sharing the OS user can write the plain store** | `check_results` is a plain SQLite table; an agent with local write access can INSERT a fake `success` **that the gate accepts at merge time** (the gate's merge-time guarantee is row-existence + head-OID match, **not** "no forgery"). A forged green is **detected only post-merge**, by a *subsequent* re-run — and no trigger fires a re-run automatically after a clean merge (STEER fires on denial; `on-commit` on new commits), so post-merge detection is a **human / out-of-scope re-run**, not an automatic structural guarantee | **Deliberate, consistent with governance's accepted-forgeable review store (R6).** The cost asymmetry holds **for fast, deterministic checks** (running is cheaper than forging-and-evading); for **slow or flaky** checks the honest run can cost more than a forged INSERT, so the deterrent rests on the structural locks (small gate-read→merge window, bootstrap-invariant, no-caller-conclusion) — runner ≠ agent + SHA-binding stand regardless | Optional provenance-signing via the retained `signature` column **only if** the threat model changes (untrusted/off-host producers) |
| **SHA-reset is correct but not atomic with the merge (TOCTOU) — incl. a local same-process window** | The gate's read (`evaluate_required_checks`) and the merge commit are two steps; a concurrent head advance could consume a result against a head it no longer matches — and **even on a single machine with no head-advance**, an agent sharing the OS user can INSERT a forged row in the gate-read→merge-commit gap (a local same-process window, within the named shared-OS-user model) | The current-head-only read shrinks the window; the residual eval-vs-commit race (forge-path **and** local same-process) is named, not assumed away | A re-resolve-at-commit guard + "require up to date" + merge-queue (deferred) |
| **The clause binds the governed `but` merge only** | A forge-side / raw-`git`-push merge consults no local check | Same accepted-leak class as governance R11/R14; the governed merge is gated, the bypass paths are named | Server-side pre-receive + forge-side required-checks (the governance closure) |
| **No human-in-the-loop is forced for a required-set change** (the `actions` PRD proposed one) | An agent that can satisfy the *current* required checks can land a change that weakens the *future* required-set; the bootstrap-invariant forces the weakening change to pass current checks but does not force human review of the policy diff | The config path is `administration:write`-gated and the diff is reviewable; the bootstrap-invariant is the engine leg. Human-in-loop for required-set edits is a **governance**-side policy concern (deferred there), consciously not re-specified here | A governance policy layer (required-by-default presets / "this diff weakens the gate" classification) — the same deferral governance already carries |
| **The check config is itself agent-editable as a file** | An agent with `contents:write` can edit `.gitbutler/checks/*.toml` and commit it | The ref-pin blocks *same-change* weakening; the bootstrap-invariant forces a required-set change to clear the currently-required checks; a weakening change that lands governs only *future* changes (same scope line as governance R13) | None (strict scope line); the steel-trap fence is the eventual closure |
